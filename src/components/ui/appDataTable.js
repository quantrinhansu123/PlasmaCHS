/** Wrapper + table class — giao diện bảng chuẩn (header xám nhạt, hàng trắng, badge pill) */
export const appDataTableWrapperClass =
  'overflow-x-auto rounded-xl border border-slate-200 bg-white shadow-sm';

export const appDataTableClass = 'app-data-table w-full border-separate border-spacing-0 text-left';

export const appTableThClass =
  'px-4 py-3 text-[13px] font-semibold text-slate-600 whitespace-nowrap';

export const appTableTdClass = 'px-4 py-3 text-sm text-slate-800 align-middle whitespace-nowrap';

export const appTableCheckboxClass =
  'h-4 w-4 rounded border-slate-300 text-[#2563eb] focus:ring-[#2563eb]/20 cursor-pointer';

/** Nút icon trong cột Thao tác — ghi đè global button (index.css) */
export const appTableActionBtnClass =
  '!inline-flex !h-8 !w-8 !min-h-0 !min-w-0 !items-center !justify-center !rounded-md !p-0 !shadow-none transition-colors';

export const appTableCellIconBoxClass =
  'flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-blue-50 text-blue-600';

export const appTableCellWithIconClass = 'inline-flex items-center gap-2.5 min-w-0';
